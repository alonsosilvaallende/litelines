[project]
name = "litelines"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "outlines-core==0.1.14",
    "numpy",
    "graphviz",
    "pydantic",
    "xxhash",
]
